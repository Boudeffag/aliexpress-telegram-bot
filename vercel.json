{
  "build": {
    "use": "@vercel/python",
    "config": {
      "pipCommand": "pip install -r requirements.txt"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
